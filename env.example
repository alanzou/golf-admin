# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/golf_admin_db"

# Redis Configuration (Upstash or local Redis)
KV_REST_API_URL="https://your-redis-url.upstash.io"
KV_REST_API_TOKEN="your-redis-token"

# JWT Secret (REQUIRED - Generate a strong secret key)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Next.js Configuration
NEXT_PUBLIC_ROOT_DOMAIN="localhost:3000"
NODE_ENV="development"

# Optional: External Services
# SENTRY_DSN="your-sentry-dsn"
# ANALYTICS_ID="your-analytics-id"

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all placeholder values with actual values
# 3. Generate a strong JWT_SECRET using: openssl rand -base64 32
# 4. Never commit .env.local to version control 